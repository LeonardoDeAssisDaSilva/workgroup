<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
      <h6>
        <%= "#{@user.course}" %>
      </h6>
      <h6>
        <%= "#{@user.organization}" %>
      </h6>
    </section>
  </aside>
</div>
