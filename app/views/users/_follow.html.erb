<% @user ||= User.find(params[:id]) %>
<%= form_for(current_user, :url => follow_user_path(@user.id), :method => :post) do |f| %>
  <%= f.submit "Seguir", class: "btn btn-custom btn-default" %>
<% end %>
