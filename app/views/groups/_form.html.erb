<%= form_for(@group) do |f| %>
  
  <%= f.label :name, "Nome do Grupo" %>
  <%= f.text_field :name, class: 'form-control' %>

  <%= f.label :area, "Área de Interesse" %>
  <%= f.text_field :area, class: 'form-control' %>

  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>
